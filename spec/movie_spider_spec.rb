require 'spec_helper'

describe MovieSpider do
  it 'has a version number' do
    expect(MovieSpider::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
